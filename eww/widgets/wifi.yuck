(defwidget wifi [state ?ssid icon_disabled icon_nonetwork icon_connected]
  (box :orientation "v"  
             :tooltip ssid 
             (label :text {state == "disabled" ? icon_disabled : 
                           ssid == "" ? icon_nonetwork :  icon_connected})))
  



