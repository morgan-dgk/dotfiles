-- Generate shareable file permalinks (with line ranges) for github web frontend, for easily sharing code locations: https://github.com/ruifm/gitlinker.nvim

return {
  'ruifm/gitlinker.nvim',
  requires = 'nvim-lua/plenary.nvim',
  opts = {}
}
